server:
  port: ${SERVER_PORT:28181}

spring:
  application:
    name: stock-consumer

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:13306}/DOVE_STOCK?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${DB_USERNAME:dove_local_test}
    password: ${DB_PASSWORD:dove1234}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1200000
      auto-commit: true

  kafka:
    bootstrap-servers: ${KAFKA_HOST:localhost}:${KAFKA_PORT:9092}

  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

redis:
  host: ${REDIS_HOST:localhost}
  port: ${REDIS_PORT:6379}

distributed-lock:
  wait-time: ${DISTRIBUTED_LOCK_WAIT_TIME:5}
  lease-time: ${DISTRIBUTED_LOCK_LEASE_TIME:60}
  ttl-seconds: ${DISTRIBUTED_LOCK_TTL_SECONDS:86400}

logging:
  level:
    root: INFO
    com.dove: INFO
    org.hibernate.SQL: WARN
    org.hibernate.type.descriptor.sql: WARN
    com.zaxxer.hikari: INFO
    org.apache.kafka: WARN
    org.springframework.kafka: WARN
